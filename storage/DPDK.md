由一系列库和驱动程序组成的框架，旨在大幅提升网络应用程序处理数据包的速度和效率
在传统的网络数据处理流程中，网络接口控制器（NIC）接收到的数据包需要经过操作系统的内核网络协议栈进行一系列复杂的处理，这个过程涉及多次内存拷贝和上下文切换（从内核态到用户态），在高网络负载下会成为严重的性能瓶颈。

DPDK的核心思想就是绕过（Bypass）操作系统的内核，让应用程序直接在用户空间（User Space）中接管网卡，从而消除上述的性能开销。这种机制被称为内核旁路（Kernel Bypass）。